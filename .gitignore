node_modules/
dist/
.turbo/
*.log
.env
.env.local
.env.e2e.local
.env.server-test.local
.DS_Store
coverage/
test-results/
playwright-report/
.playwright/
.env.bak-*

# Private overlays (open-core Option 2 — hide existence). These are SEPARATE upstream checked out
# into the CE tree by scripts/dev-bootstrap.sh; the public CE repo must contain NO trace of them.
# EE source, the landing page, and the docs site all live here and are NEVER published with CE.
/ee/
/lp/
/docs-site/

# Local tooling checkouts (not an overlay; Wikistead's own MCP is built into apps/server).
/mcp/
# dev-bootstrap writes the upstream URLs here (never committed).
/.dev-repos.env

# TypeScript incremental build state (build artifact — never track; caused perpetual diff churn).
*.tsbuildinfo

