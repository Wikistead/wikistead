// Types for the plain-JS guard next door, so a TypeScript entry point (the migration runner) can
// import it without loosening its own settings.
export declare function assertStackTarget(url: string | undefined, what: string): void;
